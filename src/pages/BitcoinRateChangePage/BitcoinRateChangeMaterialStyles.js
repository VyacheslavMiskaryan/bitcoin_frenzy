import { makeStyles } from '@material-ui/core/styles';

const BitcoinRateChangePageMaterialStyles = makeStyles(({
  rateButton: {
    width: 170,
    marginTop: 50,
    marginLeft: 10,
    marginRight: 20,
    fontSize: 12,
  },
}));

export default BitcoinRateChangePageMaterialStyles;
