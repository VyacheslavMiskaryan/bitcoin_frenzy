import { makeStyles } from '@material-ui/core/styles';

const OperationsWithMyBitcoinsMaterialStyles = makeStyles(() => ({
  button: {
    marginBottom: 30,
    borderRadius: 15,
  },
}));

export default OperationsWithMyBitcoinsMaterialStyles;
