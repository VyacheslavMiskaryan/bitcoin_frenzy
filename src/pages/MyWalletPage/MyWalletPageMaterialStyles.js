import { makeStyles } from '@material-ui/core/styles';

const MyWalletPageMaterialStyles = makeStyles(() => ({
  button: {
    marginBottom: 30,
    borderRadius: 15,
  },
}));

export default MyWalletPageMaterialStyles;
